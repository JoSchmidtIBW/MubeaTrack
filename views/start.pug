style.
    button:hover {
    transform: translateY(-3px);
    box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.15);
    }

    button{
        font-size: 1.6rem;
        padding: 1.4rem 3rem;
        border-radius: 10rem;
        text-transform: uppercase;
        display: inline-block;
        text-decoration: none;
        position: relative;
        -webkit-transition: all 0.4s;
        transition: all 0.4s;
        font-weight: 400;
        -webkit-backface-visibility: hidden;
        backface-visibility: hidden;
        /*Add later when we use this for the button in form*/
        border: none;
        cursor: pointer;
    }

body(style="position: relative;")
    img(src="/img/start/leererRaum.jpg", alt="Leerer Raum", style="width: 100%;height: 100%;object-fit: cover;")
    div(style="position: absolute; top: 10%; left: 10%;")
        a(href="/api/v1/login")
            //-button(style="border: none;cursor: pointer;font-size: 1.6rem;padding: 1.4rem 3rem;border-radius: 10rem;text-transform: uppercase;display: inline-block;text-decoration: none;position: relative;-webkit - transition: all 0.4s;transition: all 0.4s;font-weight: 400;-webkit - backface - visibility: hidden;backface-visibility: hidden;") Go to login
            button(style="background-color: #55c57a; color: white;") Go to login
        br
        br
        br
        br
        br
        a#getJoke(href="#", onclick="loadJoke()", style="color: gray;")
            //h3 Nächsten Witz anzeigen!
            //-button(style="color: #7c7875;border: none;cursor: pointer;font-size: 1.6rem;padding: 1.4rem 3rem;border-radius: 10rem;text-transform: uppercase;display: inline-block;text-decoration: none;position: relative;-webkit - transition: all 0.4s;transition: all 0.4s;font-weight: 400;-webkit - backface - visibility: hidden;backface-visibility: hidden;") Show next joke
            button(style="background-color: #ffd899; color: white; font-size: 1.2rem;padding: 1.2rem 2rem;") Show next joke
        h2#joke(style="color: #7c7875;")

    script.
      async function loadJoke () {
        try { //hier wäre fehler status 400
          const response = await fetch('https://api.chucknorris.io/jokes/random');
          console.log(response);
          if (response.ok) {
            const data = await response.json();
            console.log(data);
            console.log(data.value);
            document.getElementById('joke').innerText = data.value;
          } else {
            console.error(await response.text);
            document.getElementById('joke').innerText = "Kein Internet!!!";
          }
        } catch (e) {
          console.error(e);
          document.getElementById('joke').innerText = "Kein Internet!!!";
        }
      }